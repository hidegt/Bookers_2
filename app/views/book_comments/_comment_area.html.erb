<%= form_with(model:[book, book_comment],url: book_book_comments_path(book.id), method: :post, remote: true) do |f| %>
  <%= f.text_area :comment, rows:'5', class: "form-control" %>
  <%= f.submit "Send" %>
<% end %>
